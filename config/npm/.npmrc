# Supply-chain hardening: only install registry versions published at least 30 days ago.
# Requires npm 11+. Lockfile-pinned versions are unaffected.
# Override per-command: npm install --force
min-release-age=30